doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/topic') Server Side JavaScript
    ul
      each topic in topics
        li
          - rid = encodeURIComponent(topic['@rid'])
          a(href='/topic/'+rid)=topic.title

    artcle
      if topic
        h2= topic.title
        h3= topic.description
        if topic.author
          div= 'writen by' + topic.author
      else
        h2  Welcome
        | This is Main Page
    div
      a(href='/topic/add') 글쓰기
